{{- with .Values.ratingservice.secretData }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $.Values.ratingservice.secretName }}
  namespace: {{ $.Values.namespace }}
type: Opaque
data:
  {{- range $key, $value := . }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}
{{- end }}