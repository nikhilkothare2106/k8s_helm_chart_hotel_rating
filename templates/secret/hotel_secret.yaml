{{- with .Values.hotelservice.secretData }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $.Values.hotelservice.secretName }}
  namespace: {{ $.Values.namespace }}
type: Opaque
data:
  {{- range $key, $value := . }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}
{{- end }}